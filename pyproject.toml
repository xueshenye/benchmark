[project]
name = "multi-turn-agent-benchmark"
version = "0.1.0"
description = "Programming-domain multi-turn agent benchmark built on Harbor"
requires-python = ">=3.12"
dependencies = [
    "harbor",
    "litellm>=1.96.0",
    "novita-sandbox>=2.0.8",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
