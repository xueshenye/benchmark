schema_version = "1.3"
artifacts = []

[task]
name = "benchmark/support-bot"
description = "Interactive multi-turn task: customer-service bot (KB + order API + batch refactor + i18n/escalation) grown across 4 user rounds"
authors = []
keywords = ["multi-turn", "interactive", "cli", "customer-service", "kb", "api", "refactor"]

[metadata]

[verifier]
timeout_sec = 600.0
collect = []

[verifier.env]

[agent]
timeout_sec = 3600.0

[environment]
network_mode = "public"
build_timeout_sec = 900.0
os = "linux"
mcp_servers = []
workdir = "/workspace"

[environment.env]

[solution.env]
